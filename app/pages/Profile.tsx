"use client";
import React, { useState } from "react";
import { Box, Button, Typography } from "@mui/material";

const Profile = () => {
  return <Typography>Welcome to Profile</Typography>;
};

export default Profile;
